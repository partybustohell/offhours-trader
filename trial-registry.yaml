# Typed trial registry — the machine-readable source of truth for the quant
# testing plan (docs/QUANT-TESTING-PLAN.md). Every ALPHA-signal test is
# pre-registered here BEFORE its results are read. Two gates enforce it:
# `pnpm preflight` refuses to start if an ENABLED alpha flag has no type:alpha
# row whose `flag` matches exactly, and `backtest.ts sweep` refuses to run a
# search whose flags have no type:alpha coverage (`flag` or `flags` list).
# Both gates additionally require the covering row to carry a `mechanism`
# statement (below) before it authorizes NEW work.
#
# Field semantics:
#   date   = registration date (when the row was written), NOT the data window.
#   window = data window evaluated, e.g. "2026-01-01..2026-07-01".
#   cells  = evaluated config cells the row covers (default 1). nTrials for the
#            deflated-Sharpe multiple-testing gate = sum of `cells` over
#            type:alpha rows. Counting is PER CELL: every priced evaluation of
#            a config cell on the window counts, including re-runs under
#            changed harness/gate settings and runs later judged invalid
#            (over-counting only raises the DSR benchmark; under-counting is
#            the failure mode).
#   flags  = additional flags a campaign row covers. Registers BACKTEST SEARCH
#            only; enabling a signal LIVE requires a dedicated row whose `flag`
#            is that signal's config path.
#   mechanism = REQUIRED for a row to authorize NEW work (a live enable or a
#            new backtest search). Three sentences, each >= 5 words:
#              counterparty: who is on the other side of the trade
#              whyTheyPay:   why they systematically lose or pay
#              friction:     what stops professionals from closing it
#            If these cannot be written down, the idea is parameter-fishing
#            and must not enter the registry. Legacy rows without a mechanism
#            still count toward nTrials (history is history) but no longer
#            authorize new sweeps or enables — the gates refuse them.
#
# To test a signal: add a row { flag: <config path>, type: alpha, status:
# pre-registered, mechanism, enabledDate, horizonDays, targetActiveN } BEFORE
# enabling it. To run a backtest search: add (or bump `cells` on) a campaign
# row WITH a mechanism BEFORE reading results. Post-hoc registrations are a
# rule violation — mark them.
trials:
  - id: rev2-sweep
    date: "2026-07-09"
    window: "2026-01-01..2026-07-01"
    flag: conviction_threshold
    flags: [agent_weights.bear]
    type: alpha
    cells: 33
    status: complete
    notes: >-
      Threshold x bear-weight search on the 50-episode window: 18-cell rev1
      grid (6 thresholds x 3 bear weights, tag defaults) + 15-cell rev2 5x3
      replay -> 0.55 shipped; no positive edge established at any threshold.
      Date previously recorded as "2026-01" — that was the data-window start,
      not the registration date; corrected 2026-07-10.
  - id: quant-p0-guardrails
    date: "2026-07-09"
    flag: p0-guardrails
    type: guardrail
    status: shipped
    notes: "deploy_priority, min-position floor, max-open-names, timing blackout, gross/net caps; safe-by-construction, always-on."
  - id: quant-p1-p3-machinery
    date: "2026-07-09"
    flag: p1-p3-machinery
    type: guardrail
    status: shipped-flag-off
    notes: >-
      All P1-P3 signals built inert in the LIVE config. Backtest signal-toggle
      sweeps HAVE since been run against these flags (rows sig-sweep-1-6 and
      sig7-sweep). Enabling ANY signal live still requires a new type:alpha row
      for its specific flag first.
  - id: sig-sweep-1-6
    date: "2026-07-10"
    window: "2026-01-01..2026-07-01"
    flag: signal-toggle-sweep
    flags:
      - signals.anti_chase
      - signals.amihud
      - signals.dispersion
      - signals.trend_gate
      - signals.gap
      - signals.low_vol
      - regime.trend
      - regime.vol
      - regime.gross
      - portfolio.target_vol
      - portfolio.inverse_vol
      - execution.cost_scalar
      - execution.participation
    type: alpha
    cells: 59
    status: invalid
    notes: >-
      REGISTERED POST-HOC (runs 2026-07-09) — rule violation, logged for the
      honest nTrials count. Tags sig1-sig6: baseline + one-signal-on cells at
      threshold 0.55 / bear 1.2. Priced cells: sig1 14, sig2 3, sig3 14,
      sig4 14, sig5 14, sig6 0 (budget pass only). Broken-harness runs
      (BACKTEST_QUOTE_FEED unset -> IEX quote-blind until d1b5196, entry
      blackout on, aborted episodes); results are NOT valid evidence for any
      signal, but the cells were evaluated and count as trials.
  - id: sig7-sweep
    date: "2026-07-10"
    window: "2026-01-01..2026-07-01"
    flag: signal-toggle-sweep
    flags:
      - signals.anti_chase
      - signals.amihud
      - signals.dispersion
      - signals.trend_gate
      - signals.gap
      - signals.low_vol
      - regime.trend
      - regime.vol
      - regime.gross
      - portfolio.target_vol
      - portfolio.inverse_vol
      - execution.cost_scalar
      - execution.participation
    type: alpha
    cells: 14
    status: complete-confounded
    notes: >-
      REGISTERED POST-HOC (run 2026-07-09). Tag sig7: baseline + 13 one-signal
      cells, SIP feed, no-blackout. Baseline completed 41 episodes vs 50 in
      signal cells — sweep-results.json compares UNPAIRED totals, and failures
      censor exactly the would-trade days, so per-signal differences are a
      missing-episode artifact. No keep/kill verdict is valid until re-run
      with paired episode sets.
  - id: sip-guard-reruns
    date: "2026-07-10"
    window: "2026-01-01..2026-07-01"
    flag: headline-reruns
    type: alpha
    cells: 4
    status: complete
    notes: >-
      REGISTERED POST-HOC (runs 2026-07-09). Headline replays of the shipped
      config under feed/guard variants: tags v2-sip, v3-sip, v3b-sip,
      v3c-guard. Each is a distinct evaluated cell on the same window.
  - id: earnings-underreaction-smallcap
    date: "2026-07-10"
    flag: signals.event_reaction
    type: alpha
    status: pre-registered
    horizonDays: 180
    targetActiveN: 50
    mechanism:
      counterparty: >-
        Holders of thin small-cap names who sell into the first hours after a
        positive earnings surprise: retail profit-takers anchored to the
        pre-announcement price, plus index and ETF flows that trade on
        schedule rather than on the news content.
      whyTheyPay: >-
        Anchoring makes them treat the old price as fair value and exit too
        early, so the new information is absorbed over days instead of
        minutes — the documented post-earnings-announcement drift, strongest
        where analyst coverage is thin.
      friction: >-
        The names are too small for institutional capacity: a desk that must
        deploy millions moves the price against itself before the edge pays,
        and thin sell-side coverage means nobody is paid to close the gap
        quickly.
    notes: >-
      Registered at DESIGN STAGE (2026-07-10): the signals.event_reaction
      machinery does not exist yet and nothing has been evaluated — this row
      states the hypothesis and its mechanism before any code or search.
      Predicted locus (pre-registered subgroup): small, thinly covered names;
      an effect concentrated in large caps falsifies the friction sentence
      even at positive P&L. Set enabledDate when the implementation lands
      (before any result is read); any backtest search against this flag
      requires bumping cells here or appending a campaign row first.
  - id: insider-cluster-smallcap
    date: "2026-07-13"
    flag: signals.insider_cluster
    type: alpha
    status: pre-registered
    horizonDays: 180
    targetActiveN: 50
    mechanism:
      counterparty: >-
        Slow-to-update holders of small, thinly covered names who do not
        reprice when a corporate insider makes an unscheduled, information-
        driven open-market purchase and discloses it on an SEC Form 4.
      whyTheyPay: >-
        Opportunistic insiders buy on genuine private information the market
        absorbs over months, while most Form 4 buys are routine calendar-
        pattern trades carrying no signal, so the crowd underweights the
        informative subset and updates too slowly.
      friction: >-
        The signal is buried and must be separated from routine trades by
        classifying each insider's multi-year filing history, and the effect
        concentrates in names too small and illiquid for institutional desks
        to size into.
    notes: >-
      Registered at DESIGN STAGE (2026-07-13): the signals.insider_cluster
      machinery does not exist yet and nothing has been evaluated — this row
      states the hypothesis and its mechanism before any code or search.
      Source: Cohen, Malloy & Pomorski (2012), "Decoding Inside Information,"
      J. Finance 67(3):1009-1043 — opportunistic long-short 82 bps/mo VW
      (t=2.15), routine subset ~zero. GROSS, in-sample 1989-2007, treat as a
      PRE-DECAY UPPER BOUND: Wintoki (2016) puts the alpha in the earlier
      subperiod and Jacobs & Müller (2020) find US anomalies lose ~60% post-
      publication. Predicted locus (pre-registered subgroup): small, thinly
      covered names entered on opportunistic CLUSTER buys, long-only — small-
      cap borrow >30%/yr makes the short side uncapturable. Falsifiers: an
      effect concentrated in large caps falsifies the friction sentence; an
      effect equal across routine and opportunistic trades falsifies whyTheyPay,
      even at positive P&L. Data: free SEC EDGAR Form 4 feed. Correlated with
      earnings-underreaction-smallcap and revision-drift-lowcoverage (same
      universe, same limits-to-arbitrage) — NOT an independent bet; test as one
      thesis with three triggers. Set enabledDate when the implementation lands
      (before any result is read); any backtest search against this flag
      requires bumping cells here or appending a campaign row first.
  - id: revision-drift-lowcoverage
    date: "2026-07-13"
    flag: signals.revision_drift
    type: alpha
    status: pre-registered
    horizonDays: 90
    targetActiveN: 50
    mechanism:
      counterparty: >-
        Holders of thinly covered small caps who do not reprice when a lone
        sell-side analyst issues a forecast revision that moves away from the
        prevailing consensus estimate.
      whyTheyPay: >-
        With few analysts on the name a revision diffuses slowly instead of
        being arbitraged within minutes, so the crowd stays anchored to the
        stale consensus and updates over weeks rather than at once.
      friction: >-
        Low analyst coverage is itself the limit to arbitrage — nobody is paid
        to close the gap quickly — and the names are too small and wide-spread
        for institutions to harvest at size.
    notes: >-
      Registered at DESIGN STAGE (2026-07-13): the signals.revision_drift
      machinery does not exist yet and nothing has been evaluated — this row
      states the hypothesis and its mechanism before any code or search.
      Source: Gleason & Lee (2003), "Analyst Forecast Revisions and Market
      Price Discovery," The Accounting Review 78(1):193-225 — price adjustment
      is slower and less complete for low-coverage firms; drift concentrates in
      away-from-consensus ("high-innovation") revisions. GROSS, mid-1990s data,
      PRE-DECAY UPPER BOUND (earnings-revision momentum attenuated post-2000;
      Jacobs & Müller 2020 US post-publication decay). Predicted locus (pre-
      registered subgroup): low-coverage small caps, entry on away-from-
      consensus revisions only, hold weeks-to-a-quarter, long-only. Falsifiers:
      an effect concentrated in high-coverage names falsifies the friction
      sentence; an effect equal for herding and away-from-consensus revisions
      falsifies whyTheyPay. Data: paid estimate/revision feed required (the cost
      of the signal). Correlated with earnings-underreaction-smallcap and
      insider-cluster-smallcap (same universe) — NOT an independent bet. Set
      enabledDate when the implementation lands (before any result is read);
      any backtest search against this flag requires bumping cells here or
      appending a campaign row first.
  # ---- 2026-07-27: execution-hygiene enables. SEQUENCING VIOLATION LOGGED:
  # these four flags were enabled and deployed the morning of 2026-07-27 and
  # registered here the same day AFTER the fact (the preflight gate caught it).
  # Registered honestly rather than backdated. They are cost controls on our
  # own order flow, not edge hypotheses — the mechanism sentences below say so
  # plainly: the "counterparty" is whoever we pay the spread to, and there is
  # no friction to defend because nothing here predicts returns.
  - id: exec-cost-scalar-2026-07-27
    date: "2026-07-27"
    flag: execution.cost_scalar
    type: alpha
    status: enabled
    enabledDate: "2026-07-27"
    mechanism:
      counterparty: >-
        Liquidity providers quoting the spread we would otherwise cross at
        full size when the book is wide.
      whyTheyPay: >-
        They do not pay us — we pay them half the spread per crossing order,
        and shrinking size when the quoted spread is wide mechanically reduces
        that payment on our own flow.
      friction: >-
        None claimed and none needed: this is cost avoidance on our own
        orders, not a return prediction anyone must fail to arbitrage away.
    notes: "Down-only size haircut vs live spread (floor 0.5, 45bps ref). Evaluate in the soak via realized spread paid per trade, not P&L."
  - id: exec-participation-2026-07-27
    date: "2026-07-27"
    flag: execution.participation
    type: alpha
    status: enabled
    enabledDate: "2026-07-27"
    mechanism:
      counterparty: >-
        The displayed top-of-book liquidity our marketable child order would
        sweep through when it exceeds the quoted size.
      whyTheyPay: >-
        Nobody pays us — walking the book past the displayed size pays MORE
        than the touch, and capping the child at 25% of displayed size bounds
        that worst-case impact on our own executions.
      friction: >-
        None claimed: impact avoidance on our own flow is not an edge and
        requires no barrier to persist.
    notes: "Qty cap at 25% of displayed take-side size. Evaluate via fill quality, not P&L."
  - id: exec-gates-by-session-2026-07-27
    date: "2026-07-27"
    flag: execution.gates_by_session
    type: alpha
    status: enabled
    enabledDate: "2026-07-27"
    mechanism:
      counterparty: >-
        Off-hours books quoting wide or stale markets where a flat 50bps gate
        admitted entries the regular-session gate never would.
      whyTheyPay: >-
        Nobody pays us — session-calibrated spread, quote-age, and top-size
        gates refuse executions whose round-trip cost predictably exceeds any
        plausible edge, avoiding a known cost rather than earning a return.
      friction: >-
        None claimed: refusing bad fills on our own flow needs no protection
        from arbitrage.
    notes: "SIP-only session gates (rth 20bps/20s/100, pre+ah 80bps/90s/100). WATCH: min_top_size units (shares vs round lots) — health report flags entry starvation."
  - id: exec-entry-aggressiveness-2026-07-27
    date: "2026-07-27"
    flag: execution.entry_aggressiveness
    type: alpha
    status: enabled
    enabledDate: "2026-07-27"
    mechanism:
      counterparty: >-
        The passive side of the spread that earns the half-spread whenever our
        entry order crosses instead of resting.
      whyTheyPay: >-
        They stop being paid by us — resting halfway inside the spread
        converts us from taker to part-maker on fills, trading certainty of
        execution for lower cost per executed share.
      friction: >-
        None claimed: the trade-off is unfilled entries (re-evaluated next
        tick, no chasing), a cost we accept on our own flow, not an edge.
    notes: "0.5 = rest mid-spread. Evaluate via fill rate + effective spread paid; if fill rate collapses the setting reverts to 1.0 (pre-committed fallback)."
  # ---- 2026-07-27: catalyst-first candidate sourcing (REGISTERED BEFORE the
  # code path first runs in production — the flag ships enabled with this row).
  - id: earnings-scan-2026-07-27
    date: "2026-07-27"
    flag: universe.earnings_scan
    type: alpha
    status: enabled
    enabledDate: "2026-07-27"
    horizonDays: 180
    targetActiveN: 50
    mechanism:
      counterparty: >-
        Holders who reprice earnings news slowly after the print, and the
        movers-list crowd that only discovers a name AFTER its reaction has
        already run.
      whyTheyPay: >-
        Post-earnings-announcement drift: anchored holders absorb the surprise
        over days, so evaluating scheduled reporters BEFORE the print lets the
        panel act in the first hours of the reaction instead of nominating
        yesterday's movers after the easy part is gone.
      friction: >-
        The desk's off-hours execution window is exactly where institutional
        size cannot comfortably operate, and movers-list-driven retail flow
        arrives structurally late by construction.
    notes: >-
      Shares its economic thesis with the pre-registered
      earnings-underreaction-smallcap row (signals.event_reaction) — this is
      the INFORMATION-PROVISION half (calendar into the analyst scans; the
      panel still decides), not the deterministic signal half. Fail-open
      Nasdaq calendar. Evaluate: fraction of entries originating from
      scan-sourced nominations and their outcomes vs movers-sourced, in the
      feedback report, once >=50 OOS trades exist.
  # ---- 2026-07-27: soak reset + PRE-REGISTERED DECISION RULES for the window.
  - id: soak-reset-2026-07-27
    date: "2026-07-27"
    flag: soak-2026-07-27
    type: guardrail
    status: in-force
    flags:
      - exit_engine.trail_debounce
      - entry_cooldown_after_exit
      - judge_cache
      - execution.earnings_guard
      - universe.news_content
      - model.analysts_by_name
    notes: >-
      Soak window reset 2026-07-27 (commit a809bb1 + Tier-1/2 follow-up):
      threshold 0.50, half sizes, SIP bars + $50M consolidated ADV floor,
      7-day thesis lookup / 14-day exit-entry lookup / exits_only ticks, OCO
      exit legs, trail debounce, re-entry cooldown, judge cache, +1% breakeven
      arm removed, earnings guard, news content, shadow signal logging.
      PRE-REGISTERED DECISION RULES (written before any outcome data from this
      window exists):
      (1) MODEL FREEZE — analysts/executor claude-sonnet-5, synthesizer
      claude-opus-4-8 are frozen for the window; these are fixed IDs with no
      dated variants (verified 2026-07-27), so the control is this rule: ANY
      model change resets the OOS closed-trade counter to zero.
      (2) ENABLE ORDER — first candidate regime.trend, second
      signals.anti_chase; ONE signal per >=50-OOS-trade window; a signal is
      enabled only if its SHADOW log over the window shows its interventions
      would have improved outcomes (for regime.trend: trades taken while the
      shadow regime read hostile/risk_off underperformed trades taken while
      benign; for anti_chase: entries it would have haircut underperformed
      those it would not have).
      (3) KILL RULE — a signal whose shadow interventions correlate with
      BETTER-performing trades (it would have cut winners) is killed without
      a live trial.
      (4) CALIBRATION/WEIGHTS — apply the feedback report's table/weights at
      most QUARTERLY, only when closedTrades >= calibration.min_trades, by
      manual config edit.
      (5) JUDGE REMOVAL — if avgForgone across >=30 scored judge vetoes sits
      within +/-0.5% (no predictive value), the judge comes OFF the entry
      path (pre-committed simplification; cost + determinism win).
      (6) analysts_by_name stays EMPTY until a per-persona model A/B is
      registered as its own row and run as a paired backtest.
  # ---- 2026-07-27 (Tier 3): machinery shipped DARK with pre-stated activation
  # tests, registered BEFORE any outcome data exists in the soak window.
  - id: scale-out-exits-2026-07-27
    date: "2026-07-27"
    flag: exit_engine.scale_out
    type: guardrail
    status: shipped-flag-off
    notes: >-
      Partial take-profit machinery: with the flag on, the target trigger
      exits target_fraction (0.5) of the position ONCE (persisted marker
      targetScaledOut in position-peaks.json) and the trail / time-stop /
      hard-stop manage the remainder; the stop ratchet re-covers the remainder
      via its qty-drift replace on the next tick. All-or-nothing remains the
      shipped behavior. ENABLE CONDITION (pre-stated): the feedback report's
      exitTriggers.target bucket must show avgPostExit3dPct >= +1.5 over >= 15
      target exits (systematic money left on the table by full target exits),
      THEN a paired backtest cell registered against this flag, THEN the
      config flip. Exit-shape change: measured like exit-engine-v1 (risk
      shape, not edge).
  - id: edgar-content-2026-07-28
    date: "2026-07-28"
    flag: universe.edgar_content
    type: alpha
    status: shipped-flag-off
    horizonDays: 180
    targetActiveN: 50
    mechanism:
      counterparty: >-
        Keyword scanners and holders who reprice a filing from its headline
        number alone, without reading the guidance and segment language in
        the primary 8-K / press-release text.
      whyTheyPay: >-
        The tradable information in a filing is often in the prose — guidance
        direction, one-off vs repeatable items, covenant or dilution language
        — which diffuses over hours-to-days because most fast money reads the
        structured fields and vendor summaries, not the primary document.
      friction: >-
        Reading primary text at scale is exactly what keyword pipelines do
        not do and human analysts cannot do fast across the whole tape, and
        the effect concentrates in the thinly covered names where nobody is
        paid to read the filing at all.
    notes: >-
      Information-provision machinery (same family as news_content /
      earnings-scan-2026-07-27): each candidate's recent 8-K or EX-99 press
      release, HTML-stripped, straight from SEC EDGAR into the verdict-round
      payload as primaryFiling. The panel still decides; no parameters to
      fit. Fail-open per ticker; sequential fetches under SEC fair-access
      limits; ticker->CIK map disk-cached 7 days. SHIPS OFF: enabling changes
      verdict prompt bytes (backtest LLM caches re-run) and is a registry
      decision — flip only with a status update here, ideally at a soak-window
      boundary so the OOS sample stays homogeneous. Evaluate like
      earnings-scan: outcomes of entries whose verdicts cited filing text vs
      not, once >= 50 OOS trades exist.
  - id: model-bridge-2026-07-28
    date: "2026-07-28"
    flag: model.shadow_analysts
    type: guardrail
    status: shipped-flag-off
    notes: >-
      Model-deprecation bridge. Rule 1 (soak-reset-2026-07-27) freezes the
      analyst/executor/synthesizer model IDs and resets the OOS closed-trade
      counter on ANY change — but the frozen IDs are vendor-controlled and
      WILL eventually be deprecated, so a hard reset is guaranteed unless
      continuity evidence exists. Machinery: set model.shadow_analysts to the
      candidate model and the pipeline re-runs the verdict round on IDENTICAL
      inputs each pass, writes verdicts-shadow-<ymd>.json, and audits
      (analyst,ticker)-paired direction agreement + mean |conviction delta|.
      Nothing shadow is ever traded; a shadow failure never blocks a thesis;
      cost is one extra verdict round per pass while enabled. PRE-STATED
      PROCEDURE: (a) enable the shadow >= 15 trading days before any swap;
      (b) at swap time, a registry row must record the measured agreement; (c)
      whether measured agreement justifies carrying the OOS counter forward
      (vs the full rule-1 reset) is a NEW registry decision made on that
      evidence — this row authorizes the measurement, not the continuity
      claim. Rule 1 remains in force: the bridge is evidence for amending it,
      never an override.
  - id: passive-exits-2026-07-28
    date: "2026-07-28"
    flag: execution.exit_passive
    type: guardrail
    status: shipped-flag-off
    notes: >-
      Passive-first exits for NON-RISK triggers (target / time_stop / judge):
      the first exit attempt rests inside the spread at
      execution.exit_passive.aggressiveness (0.5 = mid) instead of crossing;
      if the trigger re-fires on a later tick with the prior exit order still
      resting unfilled, the attempt escalates to marketable. hard_stop /
      invalidation_price / trail ALWAYS cross (urgency beats cost). Symmetric
      to the 2026-07-27 entry_aggressiveness change: cost control on our own
      flow, not an edge hypothesis — the same "counterparty is the passive
      side of the spread we stop paying" mechanism as
      exec-entry-aggressiveness-2026-07-27. Trade-off is fill risk bounded to
      ONE tick (~15 min) of delay before escalation. ENABLE CONDITION
      (pre-stated): the feedback report's executionShortfall (shipped
      2026-07-28) must show mean exit-side shortfall >= 15 bps over >= 20
      non-risk-trigger exit fills, THEN the flag flips with a registry status
      update. Measured like the other execution-hygiene rows: realized
      exit-side shortfall + escalation rate, not P&L.
  - id: soak-gate-concretization-2026-07-27
    date: "2026-07-27"
    flag: soak-gate
    type: guardrail
    status: in-force
    notes: >-
      Exact tests implemented by scripts/soak-gate.ts (pnpm gate), fixed while
      the soak window has ZERO closed trades so no threshold can be fitted to
      outcomes. Trades enter the window by OPEN date >= 2026-07-27; trades
      with no shadow record are EXCLUDED from both buckets (missing coverage
      must not masquerade as evidence). Rule 2 ENABLE test: >= 50 matched OOS
      closed trades, both buckets >= 10, and the flagged bucket's mean trade
      return strictly below the unflagged bucket's (for regime.trend, flagged
      = the shadow book-level regime read hostile or risk_off on the thesis
      date; for anti_chase, flagged = the entry's shadow record wouldFire).
      Rule 3 KILL test: same sample floors, flagged mean strictly above
      unflagged. Rule 4: calibration/weights eligible when window trades >=
      calibration.min_trades AND >= 90 days since the last registry row with
      flag calibration / status applied. Rule 5: judge REMOVE when >= 30
      scored vetoes and |avgForgone1dPct| <= 0.5; strongly negative -> KEEP;
      strongly positive exceeds the pre-commitment -> REVIEW (new decision
      required). Small-sample decision rules, not significance claims; the
      gate only prints patches — applying remains a manual config + registry
      edit that preflight then enforces.
  - id: exit-engine-v1
    date: "2026-07-15"
    window: "2026-01-01..2026-07-01"
    flag: exit_engine
    type: guardrail
    status: complete
    notes: >-
      Deterministic thesis-grounded exit engine (hard stop / invalidation price /
      target / trail / time-stop), all-or-nothing, tick-enforced + native RTH
      hard-stop leg. Guardrail: no new entry signal. Measured by paired
      counterfactual on identical entries & realized fills — risk-shape claim
      (drawdown / book vol / force-flatten-truncation rate), NOT edge. Any
      mean-P&L effect is soak-scrutinized like alpha.
      RUN 1 (2026-07-20, tag exit-engine-v1): DEFECTIVE HARNESS — the episode
      runner dropped the LLM exit levels from sim theses (fixed in ea27518), so
      the pair compared fallback-only plans and was byte-identical on all 50
      shared days (exact zero diff). Kept as the no-regression floor: engine
      enforcing fallback plans reproduces legacy exactly.
      RUN 2 (2026-07-20, tag exit-engine-v1b, post-fix): 50/50 shared episodes,
      7 trades per cell, 1/50 days differ (H stratum). Force-flatten truncation
      42.9% -> 28.6% (3 -> 2 of 7; the one intervention turned a blind boundary
      flatten + judge exit into a pre-committed target + trail exit). Max
      drawdown $106.58 -> $104.49; book vol $10.04 -> $11.22 (slightly higher);
      paired net diff: R stratum exactly 0 (bootstrap CI [0,0]), H mean +$1.91,
      design-weighted +$0.31/episode — single-day, far below
      min_trades_for_economic_claim, no economic claim ("backtest disproves
      only": nothing disproved). Verdict: no risk-shape regression; weak
      favorable truncation signal; the evidence path for the guardrail claim
      remains the live paper soak (5h episodes cannot exercise multi-day time
      stops: fallback 30h > 27h episode span).
