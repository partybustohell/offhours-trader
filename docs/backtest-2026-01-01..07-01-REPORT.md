# Backtest REPORT — 5-hour episode protocol

- run tag: `defaults`
- generated: 2026-07-08T18:34:34.798Z
- window: 2026-01-01 → 2026-07-01 (124 trading days)
- sampling rule applied: 30/30 R + 20/20 H episodes present (pre-registered drop rule: first 24 R + first 16 H of the persisted priority order)

## Read this first — fill realism

All fills are simulated (strict limit cross against SIP minute bars with a 20x per-session volume guard, no partial fills). Even with the real consolidated tape, results are **optimistic relative to real extended-hours execution**. Every economic number below inherits this caveat.

“Does it work” is three separate claims: (1) **mechanical** — zero invariant violations across all episode ticks; (2) **behavioral** — nonzero but controlled trade rate; (3) **economic** — the bar in §1.1, on shipped defaults. Anything less is reported as “does not work at defaults.”

- Mechanical: 50 episodes completed, 0 failed (halt policy auto-resume); invariant violations abort an episode, so every completed episode carries zero violations

## 1. Headline — stratum R economics (defaults, untuned)

Label: stratum R (uniform random trading days) — the headline economic number. Per-episode net P&L on fresh $50,000 capital; strata are never pooled.

| metric | value |
|---|---|
| episodes | 30 |
| trades | 0 |
| gross price P&L (total) | $0.00 |
| fees (total) | $0.00 |
| borrow (total) | $0.00 |
| net P&L (total) | $0.00 |
| net P&L (mean/episode) | $0.00 |
| 95% CI on mean/episode net P&L | [$0.00, $0.00] (bootstrap 10000 draws, seed 20260708, 95.0% level, episode resampling) |
| LLM cost (total / mean per episode) | $0.00 / $0.00 |

### 1.1 Economic bar

- T-bill carry: NOT PROVIDED (pass `tbillAnnualRate`); bar not evaluated.
- mean/episode net P&L: $0.00; mean/episode LLM cost: $0.00.

## 2. H-stratum economics

Label: **conditional on top-16% cross-sectional-dispersion days (deterministic selection)**. H is an upper bound on activity; the direction of its P&L bias is unknown and regime-dependent — never claimed as conservative or favorable. Not pooled with the headline.

| metric | value |
|---|---|
| episodes | 20 |
| trades | 0 |
| gross price P&L (total) | $0.00 |
| fees (total) | $0.00 |
| borrow (total) | $0.00 |
| net P&L (total) | $0.00 |
| net P&L (mean/episode) | $0.00 |
| 95% CI on mean/episode net P&L | [$0.00, $0.00] (bootstrap 10000 draws, seed 20260708, 95.0% level, episode resampling) |
| LLM cost (total / mean per episode) | $0.00 / $0.00 |

## 3. Design-weighted full-window estimate

| component | weight | n | mean/episode net P&L |
|---|---|---|---|
| H stratum (censused) | 20/124 | 20 | $0.00 |
| R complement | 104/124 | 30 | $0.00 |
| **design-weighted estimate** | 1 | — | $0.00 |

_H stratum censused (weight 20/124); complement estimated from R draws outside the H day set (weight 104/124). Design-weighted, not a pooled sample._

## 4. Behavior

Abstention rate (stratum R only, n=30): **100.0%** (30/30), Wilson 95% [88.6%, 100.0%]. H-stratum abstentions are reported below but never enter this rate.

| metric | R | H | combined |
|---|---|---|---|
| episodes | 30 | 20 | 50 |
| abstained episodes | 30 | 20 | 50 |
| orders placed | 0 | 0 | 0 |
| orders filled | 0 | 0 | 0 |
| fill rate | n/a | n/a | n/a |
| judge vetoes | 0 | 0 | 0 |
| halts | 0 | 0 | 0 |
| dangling at flatten (positions) | 0 | 0 | 0 |
| episodes with dangling | 0 | 0 | 0 |

### Risk-gate rejections by reason

_No risk-gate rejections recorded._

_Dangling-at-flatten counts are a lower bound on the v1 multi-day orphan-position question (episodes force-flatten at D+1 20:00)._

## 5. Attribution

Win = net trade P&L (after fees and borrow) > 0. Trades pool both strata: H days are over-represented by design, so these are NOT population win rates. No weight guidance is emitted for any subgroup with n < 30.

| analyst | wins k/n | win rate | Wilson 95% | guidance |
|---|---|---|---|---|
| fundamental | 0/0 | n/a | n/a | insufficient n — see per-trade log |
| technical | 0/0 | n/a | n/a | insufficient n — see per-trade log |
| macro | 0/0 | n/a | n/a | insufficient n — see per-trade log |
| sentiment | 0/0 | n/a | n/a | insufficient n — see per-trade log |
| bear (shorts only) | 0/0 | n/a | n/a | insufficient n — see per-trade log |

### Per-trade log

_No trades._

## 6. Sensitivity (descriptive sweep)

| cell | threshold | bear | episodes | abstained | placed | filled | fill rate | trades | net P&L |
|---|---|---|---|---|---|---|---|---|---|
| t45-b8 | 0.45 | 0.8 | 50 | 49 | 0 | 0 | n/a | 0 | $0.00 |
| t45-b12 | 0.45 | 1.2 | 50 | 49 | 0 | 0 | n/a | 0 | $0.00 |
| t45-b16 | 0.45 | 1.6 | 50 | 49 | 0 | 0 | n/a | 0 | $0.00 |
| t50-b8 | 0.5 | 0.8 | 50 | 49 | 0 | 0 | n/a | 0 | $0.00 |
| t50-b12 | 0.5 | 1.2 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t50-b16 | 0.5 | 1.6 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t55-b8 | 0.55 | 0.8 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t55-b12 | 0.55 | 1.2 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t55-b16 | 0.55 | 1.6 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t60-b8 | 0.6 | 0.8 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t60-b12 | 0.6 | 1.2 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t60-b16 | 0.6 | 1.6 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t65-b8 | 0.65 | 0.8 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t65-b12 | 0.65 | 1.2 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t65-b16 | 0.65 | 1.6 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t70-b8 | 0.7 | 0.8 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t70-b12 | 0.7 | 1.2 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |
| t70-b16 | 0.7 | 1.6 | 50 | 50 | 0 | 0 | n/a | 0 | $0.00 |

_Descriptive only (no walk-forward at this n): abstention/fill-rate vs threshold on shared cached verdicts._

## 7. Root-cause findings — why zero trades

The null result is structural, not noise. Two independent mechanisms each suffice to prevent trading; both were isolated with the run's own artifacts.

### 7.1 The synthesis normalization ceiling (primary)

Weighted conviction divides the agreeing analysts' weight×conviction by the weight-sum of ALL responding analysts (`src/synthesis.ts`). All five analysts respond on every candidate (abstentions are `direction: none` verdicts, which count in the denominator, never the numerator). Consequence: two agreeing analysts at 0.65 conviction score ≈ 0.28; clearing the default 0.65 threshold requires 4–5 analysts in high-conviction directional agreement — which the deliberately abstention-friendly personas essentially never produce.

Measured over all ~1,050 verdicts in this run (real `computeThesisEntries`, threshold zeroed):

| threshold | candidates clearing (of 66 directional composites) |
|---|---|
| 0.45 | 1 (FDX long 0.467, 2026-03-19) |
| 0.50 | 0 |
| 0.65 (default) | 0 |

**The entire configurable threshold range (0.45–0.70) sits above the strategy's empirical conviction ceiling (0.467).** No weight tuning fixes this; it is the normalization semantics interacting with persona abstention discipline. Design options (a user decision, not a tweak): normalize over agreeing analysts with an agreement quorum; exclude `none` verdicts from the denominator; or recalibrate personas toward commitment.

### 7.2 Extended-hours quote blindness (independent secondary)

The executor's quote source is IEX (`getLatestQuotes`, production parity), but IEX operates 08:00–17:00 ET only. In the single episode that produced a thesis entry, all 37 extended-hours ticks skipped on `no quote`, and the two 08:00+ premarket ticks that found an IEX book skipped on a 1,032 bps spread (gate: 50 bps). **Production implication:** live, `getLatestQuotes` would return quotes that are stale by up to hours during 17:00–20:00 and 04:00–08:00 — the system would either not trade (with staleness checks) or trade on a book it cannot see (without them). Extended-hours execution needs a consolidated real-time quote feed (paid tier) or an explicit stale-quote policy.

## 8. Validity appendix

- **Leakage:** moot for this run — zero trades means there is no P&L for hindsight to have manufactured. The masked/unmasked probe was skipped as unnecessary; it must run before any future run that produces trades is believed.
- **Feed choices:** SIP minute bars for fills/screeners (probe: NVDA/TSLA/AAPL show 97–241 extended-hours bars/session on SIP, 0 on IEX); IEX daily bars for `marketInfoFor` parity. Production finding confirmed: IEX-volume-based `min_avg_dollar_volume` is ~30–50x stricter than face value; additionally §7.2's quote-blindness finding applies to production itself.
- **Fill model:** never exercised (no orders placed); its optimism caveat is unused this run.
- **Splits:** raw adjustment basis; no thesis-day/split-ex-date collisions arose in the 50 sampled episodes (no trades to protect regardless).
- **Survivorship:** current-active universe (12,918 assets); relevant only to discovery composition here.
- **Borrow:** hard gate never reached (no short orders); 4,974/12,918 symbols flagged easy_to_borrow.
- **Judge-cache canonicalization:** unused — no entry ever reached a judge tick in any sweep cell (fresh judge calls: 0).
- **Tick cadence:** 15-minute driver steps (production launchd parity); 6,500+ cell-episode tick sequences executed with zero invariant violations and zero crashes.
- **Halt policies:** only auto-resume was run; with zero orders, stay-halted is identical by construction.
- **Cost actuals:** 483 LLM calls, 5.23M input / 0.36M output tokens ≈ **$21 total, $0.42 per episode**; projected live operating cost ≈ $0.42/day (one pipeline run; judge costs would add only on entry days).

---

# Revision 2 addendum — after the synthesis fix (2026-07-09)

The §7.1 normalization defect was fixed (`src/synthesis.ts` rev 2: directional-weight
normalization + `min_agreeing` quorum, 291 tests green) and the identical 50 episodes
were replayed from the cached verdicts.

## Conviction distribution, before → after

| threshold | clears (old math) | clears (new math) |
|---|---|---|
| 0.45 | 1 | 29 |
| 0.55 | 0 | 16 |
| 0.60 | 0 | 2 |
| 0.65 (old default) | 0 | 1 |

The new `min_agreeing` quorum additionally suppressed 29 single-analyst composites.

## Quote-feed modes (§7.2 addressed for the backtest)

- **iex (production parity):** thesis entries now form, but essentially nothing executes —
  the live quote feed cannot see the extended-hours book. Production still needs a
  consolidated real-time feed (paid tier) or a stale-quote policy before live use.
- **sip (realism mode, labeled non-parity):** full execution path exercised below.

## Sweep under SIP quotes (50 episodes, ~40% of the half-year's trading days)

| threshold | trades | fills/placed | net P&L (pooled) | net P&L (stratum R only) |
|---|---|---|---|---|
| 0.45 | 13–14 | ~94% | −$98 to +$63 | −$32 to +$129 |
| 0.50 | 11 | 13/14 | −$158 | −$32 |
| 0.55 | 7 | 9/10 | −$192 | −$34 |
| 0.60 | 1 | 1/1 | +$3 | +$3 |
| 0.65+ | 0 | — | — | — |

Bear-weight (0.8/1.2/1.6) changed at most one trade per threshold.

**Qualitative:** the full machine now demonstrably works — 94% fill rate, negligible fees,
and judge exits with thesis-grounded reasoning (e.g. MSTR short exited on the thesis's own
stabilization-invalidation condition; TMUS long exited when price failed its stated ~190
hold level). Many exits are episode-boundary force-flattens after ~27h, truncating the
'days'-horizon theses — a pre-registered measurement artifact that shrinks |P&L| per trade.

**Economic verdict (pre-registered rule applied):** every cell is far below the 30-trades-
per-half floor, so the sweep is descriptive only. P&L at every threshold is statistically
indistinguishable from zero (±0.4% of equity over the sample). **No edge is established in
either direction.** The backtest proves the system trades correctly, not that it trades
profitably; the paper-account soak is the instrument for accumulating the trade count a
real economic verdict needs.

**Config change shipped:** `conviction_threshold: 0.55` (from 0.65) — the highest value
with a meaningful trade rate (~7 per 50 sampled days ≈ 1–2/week). `min_agreeing: 2` added.
Live trading remains gated behind the paper soak and the mode/ack/keys triple check.

---

# Revision 3 addendum — Stage 0 probes + production-parity cell (2026-07-10)

Three measurements this plan owed itself. Net effect: **every economic or
behavioral number in this report, rev 1 and rev 2 included, is now formally
inadmissible as evidence about the deployed system.** The paper soak is the
only instrument left standing (which is what the plan pre-registered anyway).

## R3.1 Cutoff verification (pre-registered T8 — previously never run)

Published cutoffs (platform.claude.com models overview, fetched 2026-07-10):
`claude-sonnet-5` (analysts/executor) and `claude-opus-4-8` (synthesizer) both
have **training data cutoff Jan 2026** (reliable knowledge cutoff also Jan
2026). Window starts 2026-01-01 → **the cutoff intrudes** (operator-supplied
conservative day: 2026-01-31; artifact `backtest-out/probe/cutoff-note.json`).

Pre-registered consequence applied: any future headline from this window must
be a fresh-start run over the **strictly post-cutoff sub-window (Feb 1 –
Jul 1)**; January episodes are labeled **leakage-contaminated**. Note the
asymmetry: Feb–Jun outcomes postdate the training data and cannot be memorized;
the contamination question is confined to January — which contains 5 of the 14
unique traded (day, ticker) pairs across all runs (JNJ 01-14, V/MA 01-16,
XLE 01-21, STX 01-27).

## R3.2 Masked/unmasked hindsight probe: POWERLESS — STOP branch taken

The rev-1 validity condition ("the probe must run before any future run that
produces trades is believed") was violated by rev 2; run now, the probe cannot
deliver: all three positive controls — event days the models certainly
memorized (NVDA 2024-05-22 earnings, SPY 2024-08-02 carry-unwind eve, TSLA
2024-04-23 earnings) — **failed to trip** on two independent personas
(sentiment: conviction divergence ≤ 0.05; fundamental: 0.10; trip threshold
0.30; no unstated outcomes cited). Artifacts:
`backtest-out/probe/controls.json`, `controls-fundamental.json`.

Interpretation: the abstention-trained personas ground verdicts in the supplied
data and do not verbalize memory, so masking numerics changes nothing **even
when memorization is certain**. The probe is powerless; a null on the real
window would bound nothing. Arms 1–2 were therefore skipped (pair inputs
retained: `backtest-out/probe/pairs-arm{1,2}.json`, built by
`scripts/backtest-probe-pairs.ts`). Pre-registered Stage-0 consequence: **no
backtest verdict is admissible; only the live soak is valid OOS.** January
contamination is now both plausible (R3.1) and unmeasurable (R3.2).

## R3.3 Production-parity cell (`parity-rth-iex`) — first simulation of the shipped config

`backtest.ts run --tag parity-rth-iex` executes the config verbatim (RTH-only
sessions, IEX quotes, entry blackout, P0 gates, threshold 0.55). A fresh-call
budget pass (`run --count-only`, added this revision) showed 0 fresh judge
calls; the priced run made no live API calls.

| metric | value |
|---|---|
| episodes | 50/50 completed, 0 failed |
| non-abstained (thesis formed) | 14 |
| orders placed / filled | **0 / 0** |
| risk-gate rejections | 0 |
| trades / net P&L | 0 / $0.00 |

**Mechanism (not a gate, a structural mismatch):** every RTH tick logs
`no_thesis` — the episode's thesis is `kind: "offhours"` (hardcoded,
`scripts/backtest-episode.ts:475`), and RTH ticks only consume a `kind: "rth"`
thesis (`src/executor-loop.ts:213`). Production runs two pipelines (17:05
evening → offhours thesis; 09:00 `pipeline rth` → rth thesis,
`scripts/install-schedule.sh`); the backtest simulates only the evening one.

Consequences:
1. **The deployed entry path (09:00 rth thesis → RTH entries) has never been
   simulated.** The rev-2 trade-rate (~1–2/week) was measured on off-hours SIP
   fills and carries no information about it. The paper soak starts with no
   backtest-derived expectation at all. *(Addressed in R3.4: the episode
   runner now simulates the morning pipeline behind `--rth-thesis`.)*
2. **Production dead weight:** with off-hours sessions disabled (2026-07-09),
   the daily 17:05 pipeline produces theses that can never execute — entries
   are impossible in every session that would consume them. Either re-point
   the evening spend or accept it as signal-history collection.
3. The zero is a *mechanical* finding about session/thesis wiring, reported
   under the same inadmissibility banner as everything else (R3.1/R3.2).

## R3.4 RTH-thesis simulation (`--rth-thesis`, tag `parity-rth-iex-rthsim`) — first measurement of the deployed entry path

The episode runner now simulates the second production pipeline: with
`backtest.ts run --rth-thesis`, at sim D+1 09:00 ET a `kind: "rth"` thesis for
D+1 is assembled through the same production entry-assembly path
(`computeThesisEntries` → `writeNarratives` → merge → borrow gate) and written
via the real `thesisPath(d1, 'rth')` with the real `rthThesisExpiry` (D+1
16:00 ET), so D+1 RTH ticks consume it exactly as `src/executor-loop.ts`
requires. Off-hours-thesis runs without the flag are unchanged.

**INFORMATION-SET APPROXIMATION (read before using any number below):** the
simulated morning thesis is built from the **same cached day-D 17:00 verdict
set** as the evening thesis. Production's 09:00 run re-runs the whole Phase A
— fresh morning mover/actives scans, nominations, verdicts — and sees
overnight news (D 17:00 → D+1 09:00). The simulation therefore gives the
morning thesis only evening information: production could pick different
tickers and convictions entirely. This choice keeps backtest LLM cost at ~zero
(52 fresh judge calls across the run; narratives and Phase A fully cached) and
was made because Stage 0 (R3.1/R3.2) makes added fidelity buy no admissible
evidence. Daily-bar features and regime are as-of-D closes, which *is* the
latest complete bar set at D+1 09:00 — those inputs are exact. Second
fidelity gap: production RTH entries carry a native broker stop-loss leg
(plus GTC conversion by ensure-stops); the sim has no resting-stop engine, so
the per-tick deterministic stop check stands in for it (fires at most one
15-minute tick late).

Run: shipped config verbatim (RTH-only sessions, IEX quotes, entry blackout,
threshold 0.55), fresh-call budget pass first (52 judge misses, limit 300,
0 non-judge misses), then the priced pass. 2026-07-10.

| metric | value |
|---|---|
| episodes | 50/50 completed, 0 failed |
| non-abstained (thesis formed) | 14 (identical to R3.3 — same verdicts) |
| orders placed / filled | 11 / 10 |
| closed trades (round trips) | **7**, on 6 unique days |
| judge vetoes | 9 |
| risk-gate rejections | 1 (`duplicate open order`) |
| force-flattened at D+1 20:00 | 4 |
| net P&L (fees incl.) | −$2.54 |
| stratum split | R: 5 trades, −$31.10 · H: 2 trades, +$28.56 |
| January (leakage-contaminated per R3.1) | 1 of 7 trades (JNJ 01-14) |

**Measured trade-rate expectation for the paper soak:** ~7 closed trades per
50 sampled episode-days (~0.7/week; 6/50 days traded). The zero in R3.3 was
wiring, not gating: once a `kind: "rth"` thesis exists, the shipped gates
(blackout, spread, quote-size, judge) pass entries at a rate comparable to the
rev-2 off-hours measurement (~7 per 50 days) despite the different session and
feed. Note the sample is dispersion-stratified (30 R + 20 H), not uniform.

**Admissibility:** per Stage 0 (R3.1/R3.2) this number is inadmissible as
evidence of edge — P&L here is noise around zero and must not be quoted as a
return expectation. Its only sanctioned uses are behavioral: (a) the deployed
path *does* place orders under shipped gates, and (b) a soak trade-rate
grossly different from ~0.7/week (after the information-set caveat above)
indicates a wiring or gating divergence worth investigating.
